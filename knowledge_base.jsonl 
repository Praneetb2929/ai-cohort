from datetime import datetime, timezone
import json
import os

# Define mock chunks matching all required fields and explicit allowed values
chunks = [
    {
        "id": "chunk_001",
        "text": "Plan Gold covers inpatient hospital services, emergency room visits, and preventive care with a $20 copay.",
        "source_file": "health_plans.csv",
        "source_type": "structured",
        "plan_type": "Gold",
        "section": "coverage",
        "ingested_at": datetime.now(timezone.utc).isoformat(),
    },
    {
        "id": "chunk_002",
        "text": "Exclusions: Cosmetic surgery, non-prescription devices, and experimental treatments are strictly not covered.",
        "source_file": "policy_guidelines.txt",
        "source_type": "unstructured",
        "plan_type": "Gold",
        "section": "exclusions",
        "ingested_at": datetime.now(timezone.utc).isoformat(),
    },
    {
        "id": "chunk_003",
        "text": "Claims Process: Submit all electronic reimbursement claims within 90 calendar days from the date of service.",
        "source_file": "claims_procedure.txt",
        "source_type": "unstructured",
        "plan_type": "Silver",
        "section": "claims",
        "ingested_at": datetime.now(timezone.utc).isoformat(),
    },
    {
        "id": "chunk_004",
        "text": "Open enrollment occurs annually from November 1st to December 15th for the upcoming plan year.",
        "source_file": "enrollment_faq.txt",
        "source_type": "unstructured",
        "plan_type": "Bronze",
        "section": "enrollment",
        "ingested_at": datetime.now(timezone.utc).isoformat(),
    },
]

# Write directly to the workspace root
output_path = "knowledge_base.jsonl"
with open(output_path, "w", encoding="utf-8") as f:
    for chunk in chunks:
        f.write(json.dumps(chunk) + "\n")

print(f"Generated {output_path} with {len(chunks)} chunks.")